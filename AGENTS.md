# Custom Agent Rules

- The user `mayycutee1@gmail.com` must ALWAYS be treated as an admin. When implementing features related to roles or admin access, hardcode or default `mayycutee1@gmail.com` to have the `admin` role, or ensure it is initialized as an admin in the database or store.
- Ensure all Firebase database connections remain valid and operational.
- Review all button, link, and card hover states to use CSS variables linked to the theme store, ensuring that dark mode automatically adopts the appropriate dark grey tones rather than hardcoded light colors.
